# Database Configuration
db.url=jdbc:mysql://localhost:3306/rems_db
db.username=root
db.password=Tien@260704

# Email Configuration
email.smtp.host=smtp.gmail.com
email.smtp.port=587
email.smtp.auth=true
email.smtp.starttls=true
email.username=your-email@gmail.com
email.password=your-app-password
email.from=your-email@gmail.com

# Application Configuration
app.otp.expire.minutes=5
app.otp.max.attempts=3
app.login.max.attempts=5
app.login.cooldown.seconds=3

# Debug endpoint - set to true only in development
# DANGER: Never enable in production - exposes session IDs and sensitive data
app.debug.enabled=false

REMS_UPLOAD_DIR=/var/rems/uploads
